```bash
ls -h # отображает параметр размера более читаемо ( в Мегабайтах )
ls -A # не показывать скрытые файлы
ls -a # показывать скрытые файлы
```
Примечательно что с утилитой `tree` это тоже работает