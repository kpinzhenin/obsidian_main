Настройки Git. Все установленные настройки проверяются командой:
```bash
git config --list
```
Существует несколько уровней применимости настроек - глобальный и другие.
имя пользователя
```bash
git config --global user.name 
```
